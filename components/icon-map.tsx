import { LigatureIcon as SignatureIcon, CreditCardIcon, Table2Icon, FolderIcon, HeadsetIcon, FileQuestionIcon } from 'lucide-react';

export const iconMap = {
  dashboard: Table2Icon,
};

export const DefaultIcon = FileQuestionIcon;
